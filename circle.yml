database:
  override:
    - echo "No database setup required..."
dependencies:
  pre:
    - gem install bundler
    - bundle config without development
  post:
    - cd spec/rails_app && bundle install
